#pragma once
#include "Trie.h"


extern void  AddElementToArray(char elements, int& size, char element) {
	//size = size + 1;
	//
	//TrieElement<T>* resizedList = new TrieElement<T>[size];

	//if (elements == nullptr)
	//	return resizedList;

	//

	//for (int i = 0; i < size; i++) {
	//	elements[i] = element;
	//}
}